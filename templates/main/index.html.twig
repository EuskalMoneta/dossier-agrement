{% extends 'base.html.twig' %}

{% block title %}Hello !{% endblock %}

{% block content %}
    <style>
        .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    </style>

    <div class="container">
        <div class="row">
            <div class="col-10">
                <h1 class="H1-header-40"> Bienvenue sur l’application de saisie des dossiers d’agréments. 👋 </h1>
            </div>
        </div>

        <div class="row mt-5">
            <div class="col-6">
                <div class="grey-square">
                    <h3 class="h3-25bold">Démarrer la saisie d’un nouveau dossier</h3>

                    <p class="paragraph-16reg">
                        Créer un nouveau dossier d’agrément, le remplir avec le pro et transmettre les données.
                    </p>

                    <div class="mt-5">
                        <a href="{{ path('app_dossier_nouveau') }}" class="btn btn-primary">Nouveau dossier</a>
                    </div>
                </div>
            </div>
            <div class="col-6">
                <div class="grey-square">
                    <h3 class="h3-25bold">Mes derniers dossiers</h3>

                    <div class="intro-18reg">
                        <ul>
                            {% for dossier in dossiers %}
                                <li class="hp-list-dossier"><a href="{{ path('app_dossier_coordonnees', {'id': dossier.id}) }}" class="linkText"> {{ dossier }} </a></li>
                            {% endfor %}
                        </ul>
                    </div>

                    <div class="d-flex flex-row-reverse">
                        <a href="{{ path('app_mes_dossiers') }}" class="linkText">
                            <svg xmlns="http://www.w3.org/2000/svg" width="30" height="30" fill="currentColor" class="bi bi-arrow-right" viewBox="0 0 16 16">
                                <path fill-rule="evenodd" d="M1 8a.5.5 0 0 1 .5-.5h11.793l-3.147-3.146a.5.5 0 0 1 .708-.708l4 4a.5.5 0 0 1 0 .708l-4 4a.5.5 0 0 1-.708-.708L13.293 8.5H1.5A.5.5 0 0 1 1 8z"/>
                            </svg>
                        </a>

                    </div>

                </div>
            </div>

        </div>

        <div class="spacer-end-of-block"></div>

        <div class="row">

            <h3 class="h3-25bold">Rechercher un dossier</h3>
            <form method="post">
                <div class="d-flex">
                    <div class="flex-grow-1">
                        <input type="text" class="form-control" name="term">
                    </div>
                    <div class="ms-4">
                        <input type="submit" class="btn btn-outline-primary" value="Rechercher">
                    </div>
                </div>

            </form>


            <div class="mt-3">
                {% if method %}
                    {% if dossiersRecherche | length > 0 %}

                        {% for dossier in dossiersRecherche %}

                            {% include 'main/miniDossier.html.twig' with {'dossier': dossier} %}
                        {% endfor %}

                    {% endif %}
                {% endif %}
            </div>
        </div>
    </div>
{% endblock %}
